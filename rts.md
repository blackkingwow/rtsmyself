一、适配 Unity 的完整版 Claude 提示词（可直接复制）
我需要你完全基于 Unity 引擎 + C# 开发一款极简 2D RTS 游戏，实现所有基础功能，代码结构规范、可直接在 Unity 中运行，严格遵守以下全部需求：
核心开发要求（Unity 专用）
所有单位、建筑全部用 Unity 基础几何体（Cube/Sphere/Capsule）代替
代码使用 C#，遵循 Unity 规范，分脚本管理（地图、单位、UI、战争迷雾、敌人 AI 等）
提供完整可直接使用的脚本 + 场景搭建说明
不使用特殊资源，全部用 Unity 自带图形（Sprite/Image/LineRenderer）实现
1. 地图系统
地图为纯白平坦平面，印有灰色标准网格
地图垂直分为：上半部分 = 敌方区域、下半部分 = 玩家部署区域
玩家只能在下半部分任意位置部署单位 / 建筑
2. 战争迷雾系统
两种状态：已照亮（可见）、未照亮（黑色半透明遮罩）
视野范围：
导弹车：低视野
雷达站：静默模式 = 小视野；主动侦察 = 大视野
骇入技能：180 秒全图永久可见，覆盖战争迷雾
使用 Unity 渲染方式实现动态迷雾更新
3. UI 布局（固定位置）
右上角：资源 UI
仅显示：金钱：XXX，实时更新
右边栏：部署 UI
部署按钮：导弹车、雷达站（建筑）
点击按钮 → 在下半部分点击位置部署
左边栏：技能 UI
骇入敌方网络：激活后 180 秒全图视野
cd:300s;
下边栏：功能按钮
导弹车：发射导弹（点击后选择敌机攻击）
雷达站：切换静默 / 主动侦察
4. 玩家单位 / 建筑
导弹车
部署花费：100金钱
可移动
低视野
攻击目标：仅敌方飞机
攻击流程：点击发射按钮 → 选中敌机 → 攻击
CD：5 秒
头顶永久显示血条
雷达站（建筑）
部署花费：100金钱
不可移动
两种状态：
静默：小视野，不被敌机锁定
主动侦察：大视野，会被敌机锁定
下边栏开关切换状态
cd:0.1s;
头顶永久显示血条
玩家基地（核心）
位于下半部分
被摧毁 → 游戏结束
被动：每分钟 +100 金钱
两个按钮：
修复：点击 → 选友方单位 → 花钱回血
cd:5s
升级基地：花钱提升每分钟金钱产出
cd:0.1s;
头顶永久显示血条
5. 敌方飞机 AI
从地图最上方随机位置刷新
生成时二选一锁定目标，之后永不改变：
目标 A：开启主动侦察的雷达站
目标 B：玩家基地
行为：
飞向目标 → 到达上空投弹 → 造成伤害（即使目标已摧毁也要飞完流程）
视觉：
飞机与目标之间用红线连接（LineRenderer），类似红警心灵探测效果
头顶永久显示血条
6. 通用规则
所有单位、建筑、敌机头顶永久显示血量
点击选中、移动、部署、技能逻辑完整
金钱系统、冷却、视野、伤害逻辑闭环
提供简单的场景搭建步骤（空对象挂载脚本即可运行）
### 补充：核心数值表（基础版）
1. 初始金钱：500
2. 血量：
   - 导弹车：200；雷达站：300；玩家基地：1000；敌方飞机：150
3. 伤害/恢复：
   - 敌方飞机投弹伤害：100/次；导弹车攻击伤害：80/次
   - 基地修复：花费 50 金钱，为选中友方单位恢复 100 血量
4. 基地升级：
   - 升级花费：300 金钱；升级后每分钟金钱产出提升至 200
5. 视野半径（Unity 世界单位）：
   - 导弹车：5；雷达站（静默）：8；雷达站（主动）：20
6. 移动/刷新：
   - 导弹车移动速度：5 单位/秒；敌方飞机飞行速度：8 单位/秒
   - 敌机刷新频率：每 60 秒随机刷新 1 架

### 补充：逻辑细节
1. 敌机逻辑：锁定目标后若目标已摧毁，投弹到目标原坐标，无伤害；
2. 选中逻辑：单选（点击单位/建筑），选中后添加高亮边框（用 Sprite 渲染器实现）；
3. 移动逻辑：导弹车选中后，点击地图任意位置，向该位置移动；

### 补充：技术实现建议
1. 战争迷雾：用 RenderTexture + 圆形遮罩实现视野，未照亮区域为黑色半透明（Alpha=0.7），实时更新视野范围；
2. 血条：UGUI 画布（Render Mode=World Space），血条预制体挂载到单位/建筑父对象，Y 轴偏移 1 单位，随对象移动；
3. LineRenderer（敌机-目标红线）：挂载到敌机对象，起点为敌机位置，终点为目标位置，每帧更新坐标，线宽 0.1，颜色纯红；

视觉与 UI 标准
地图为纯白平面，灰色网格线：颜色 #888888，线宽 1px，网格间距 1 米。
战争迷雾未照亮区域为纯黑半透明遮罩（Alpha=0.75），照亮区域完全可见。
所有单位头顶永久显示血条：使用 World Space UGUI，宽 50px、高 6px，红色血条 + 深色背景，固定显示在模型正上方。
敌机与锁定目标之间使用 LineRenderer 绘制红线：颜色 #FF0000，宽度 0.1，每帧实时更新，效果类似红警心灵探测线。
单位选中时高亮描边，未选中恢复正常；按钮冷却时变灰并显示冷却倒计时。
场景结构与搭建要求
场景必须包含：GameManager（单例）、Map、MainCamera（正交）、Canvas、Units、Enemies 层级。
相机为 2D 正交相机，适配 16:9，视野覆盖整张地图。
所有单位设置正确 Tag：PlayerBase、PlayerUnit、Enemy、Building 等，确保选中与伤害逻辑正常。
交付内容必须包含：全部 C# 脚本 + 场景搭建步骤 + 预制体配置说明，可直接导入运行。
交互体验与异常处理
金钱不足、部署到上半区、无目标攻击时，给出明确文字提示，禁止无效操作。
单位移动、部署、技能释放、按钮点击均有清晰视觉反馈。
敌机即使目标被摧毁，仍完整执行飞行与投弹动画，不中断行为。
基地被摧毁后弹出游戏结束 UI，禁用所有操作，逻辑干净不报错。
代码质量要求
脚本结构清晰，变量与方法命名语义化，关键逻辑添加注释。
不使用魔法数值，常用参数集中管理，便于后续调整。
无报错、无空引用、无内存泄漏，支持长时间运行。